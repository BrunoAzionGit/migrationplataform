{
  "name": "@azion-migration/core",
  "version": "0.1.0",
  "private": true,
  "description": "Lógica compartilhada entre módulos: clients de API (Azion v4, Cloudflare), Edge SQL, Object Storage, máquina de estados de job (Importar -> Executar), identidade best-effort via token Azion.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
